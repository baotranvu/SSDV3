# Task: Optimize Multi-stage Dockerfile for Backend API

## Meta

| Field | Value |
|-------|-------|
| **Task ID** | TASK-007 |
| **Feature Spec** | N/A (DevOps Infrastructure) |
| **Technical Spec** | N/A (Standard DevOps Practice) |
| **Size** | S |
| **Assignee** | AI Coding Agent |
| **Status** | In Progress |
| **Created** | 2026-06-12 |

---

## Objective

Optimize the existing Backend Dockerfile to use multi-stage builds, reduce image size, improve build caching, and ensure secure non-root execution with proper volume permissions for SQLite and logs.

---

## Prerequisites

- [x] Read `/docs/workflows/ai-agent-workflow.md`
- [x] Analyze current `VietnamTravel3D/Dockerfile`

---

## Implementation Steps

### Step 1: Analyze and Refactor `VietnamTravel3D/Dockerfile`

- Use `.NET 10 SDK` for building.
- Use `.NET 10 ASP.NET Runtime` for the final stage.
- Optimize `COPY` commands to leverage Docker layer caching (restore dependencies before copying source).
- Ensure `curl` is installed for health checks.
- Set up non-root user (UID 1654) and permissions for `/app/Logs` and `/app/data`.

### Step 2: Verify `docker-compose.yml` alignment

- Ensure volumes are correctly mapped.
- Ensure environment variables (`ASPNETCORE_URLS`, `ASPNETCORE_ENVIRONMENT`) match the Dockerfile.

---

## Files to Touch

| File | Action | Notes |
|------|--------|-------|
| `VietnamTravel3D/Dockerfile` | Modify | Primary optimization |
| `docker-compose.yml` | Verify | Alignment check |

---

## Acceptance Criteria

- [ ] `docker build` succeeds without errors.
- [ ] Final image size is minimized.
- [ ] Application runs successfully as non-root user (UID 1654).
- [ ] Write access to `/app/Logs` and `/app/data` is verified within the container.
- [ ] Health check via `curl` works.

---

## Implementation Checklist

### Code Quality
- [ ] Dockerfile uses multi-stage build pattern.
- [ ] Layers are optimized for caching.
- [ ] Non-essential files excluded via `.dockerignore` (if exists).

### Security
- [ ] `USER 1654` is used.
- [ ] No sensitive data hardcoded.

---

## PR Description

```markdown
## Summary
Optimized Backend Dockerfile with multi-stage build, caching improvements, and non-root security.

## Specs
- Task: TASK-007

## Changes
| File | Action | Note |
|------|--------|------|
| `VietnamTravel3D/Dockerfile` | Modified | Multi-stage optimization, caching, security |

## Testing
- [ ] Docker build successful
- [ ] Container starts and logs correctly
- [ ] Permissions verified for SQLite and Serilog
```

---

*Generated by Cline — 2026-06-12*
